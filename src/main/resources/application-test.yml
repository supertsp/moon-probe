spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/moon_tests?createDatabaseIfNotExist=true
    username: root
    password:
  jpa:
    hibernate:
      use-new-id-generator-mappings: false
    show-sql: true
    properties:
      hibernate:
        format_sql: true
#  jackson:
#    serialization:
#      write_dates_as_timestamps: false